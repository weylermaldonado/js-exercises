/**
 * Given an array of numbers implement a between function
 * that returns the value(s) between two number.
 */

const list = [1, 2, 3, 4, 5, 6, 7, 8]


console.log(list.filter(between(3, 6))) // [4,5]